# A股 5 分钟数据口径与可追溯依据

本文集中记录数据探测阶段确认的事实。`quality_check.py` 中的断言注释以条目编号回链到这里。

## DN-001：5 分钟时间戳与正常交易日时刻表

Baostock `frequency="5"` 的 `time` 原始值为 17 位字符串，例如
`20260713093500000`。前 14 位按 `%Y%m%d%H%M%S` 解析，最后三位为毫秒位。
时间戳表示 bar 结束时刻。

正常交易日共 48 根：上午 `09:35–11:30`，下午 `13:05–15:00`，每 5 分钟一根。
半日市或特殊日期不直接套用 48 根硬断言；先依据横截面市场日模式识别，再进入完整性统计。

## DN-002：历史覆盖下限

对 `sz.300308` 的 2019 全年请求返回 0 行；2020 年初请求最早返回
`2020-01-02 09:35:00`。2020–2026 年相同交易周窗口均返回 5×48 行。

正式下载器仍保留 2019 请求。若股票已上市而 2019 返回空，记录为数据源覆盖缺口。
若股票池前 20 只的 2019 段全部为空，后续股票自动跳过 2019 段。每股有效质检起点取
日线参考表首日与分钟线首个返回日的较晚者。

## DN-003：成交量、成交额与收盘价口径

`sz.300308` 最近 8 个交易日的 48 根分钟线汇总与直接不复权日线相比：

- volume 逐日完全一致；
- amount 逐日完全一致；
- 15:00 bar 的 close 与日线 close 完全一致。

据此只能推断这 8 个近期交易日的开盘和收盘集合竞价成交量、成交额已进入 48 根分钟
汇总，且极可能分别归入首根和末根 bar。由于没有逐笔数据，这是一项局部样本的对账
推断，不声称为接口正式定义。

10 只股票的 2020–2026 全历史质检显示明显的年份口径变化，不能把近期结论外推：

- 2020 年 volume 和末根 close 的逐日完全匹配率均为 100%；
- 2023 年 volume 完全匹配率降至 58.08%，close 匹配率降至 85.77%；
- 2024 年 volume 完全匹配率仅 0.05%，volume 相对误差中位数约 +0.81%；
- 2025 年 volume 完全匹配率为 6.26%，close 匹配率为 97.53%；
- 2026 年截至 8 月 6 日又恢复到 volume 99.27%、close 99.93% 完全匹配。

amount 的绝对分币级严格对账在早期年份也会捕获极小舍入差，但 2023–2025 的相对误差
P95 达到约 1.08%–1.28%，已不是单纯浮点噪声。严格异常清单和相对误差分位数
均保留；后续机制调查、否证结果和放量决策见 DN-007。

## DN-004：日线与分钟线 high/low 差异

同一探测样本中，日线 high 与分钟 high 最大差 12.05 元，日线 low 与分钟 low 最大差
0.96 元。直接请求 `adjustflag=3` 的日线后差异仍存在。

“日线极值来自开盘集合竞价、因此日线极值应接近 open”的假设未获支持：探测期
8 个 high 偏差日和 6 个 low 偏差日命中数均为 0；扩展到 10 只完整样本后，4,166 条
high 偏差和 3,953 条 low 偏差仍然 0 命中。因此 high/low 只做误差分布与明细统计，
不做严格一致断言，也不把差异强行归因于竞价。

## DN-005：日线参考表及 CTO 数据谱系

CTO 日线位于 `data/cto_baostock/daily_raw/{六位代码}.csv`，同时存在 `daily_hfq`。
现有 `daily_raw` 多数由后复权 OHLC 除以后复权因子重建，且文件内可能仍保留
`复权标记=1`。这不影响本次按成交额选择股票池，但不作为分钟价格的标准对账基准。

分钟管道为每只股票补拉直接 `adjustflag=3` 的伴生日线表，保存字段为
`date,code,open,high,low,close,volume,amount,tradestatus`。100 只试点和未来 1500 只
放量均使用同一规则。

## DN-006：缺失、停牌与特殊日期分类

直接不复权日线的 `tradestatus=0` 表示停牌。完整性分类如下：

- 日线 `tradestatus=0` 且无分钟 bar：整日停牌；
- 日线 `tradestatus=1` 且无分钟 bar：整日缺失；
- 日线 `tradestatus=1` 且有但少于正常市场日根数：日内缺根；
- 横截面多数股票共同少于 48 根：特殊市场日，进入统计而不触发 48 根硬断言。

## DN-007：系统性对账异常的机制验证与放量门禁

针对 10 只样本的 5,203 个 volume 不一致日，专项验证了“盘后固定价格交易/大宗
交易进入日线、但不进入 5 分钟线”假设。该假设对主要异常不成立：

- 假设要求日线 volume 大于分钟合计，但只有 1,299 日（24.97%）符合；3,904 日
  （75.03%）的方向相反。因此不将质检关系改为“日线 ≥ 分钟合计”。
- AkShare 大宗交易明细在样本期内有 3,507 条，其中 656 个异常日有同日记录；
  但日线减分钟的差额与大宗成交量在“100 股或 1%”容差内的匹配数为 0。
- 将正向 volume/amount 差额的隐含价格与日线 close 比较，仅 116 日在 0.1% 内。
  这只是“收盘价批次”代理，无法单凭 K 线区分盘后定价、收盘集合竞价或其他来源。
- 两种机制并集只解释 116/5,203（2.23%），剩余 5,087 日（97.77%）无法解释，
  远低于放量所需的 90% 解释门槛。

独立日线交叉验证将问题进一步定位到历史分钟端：AkShare/Sina 与 Baostock 直接不
复权日线的 volume 在 2023–2026 年每个板块的逐日匹配率均为 100%；它与分钟合计在
2024 年的匹配率为 0%–0.21%。另外，8 只非科创板样本从 2023-08-09 至 2025 年末的
每根分钟 volume 都是 100 股整数倍；2023-08-08 之前和 2026 年的大部分日期并非如此。这是历史分钟
档案曾发生数值精度或数据源切换的强证据，但不是对供应商内部实现的确证。

396 个末根 close 异常与复权边界无关：396 日全部同时发生 volume/amount 不一致，
与从 CTO 后复权/原始价比值推断的复权因子变化日重合数为 0；其中 45 日与大宗交易同日，
但无一笔成交量匹配。其中 80 日命中收盘价批次代理，不足以解释全部。2023 年异常
常在多只股票的同一日集中出现，也支持供应商历史档案的系统性不一致，而非个股复权事件。

决策：保留 volume、amount、末根 close 的严格等值对账，不用任意误差阈值将系统性
差异降级。这里并不预先要求迁移整条管道；可以先通过第二分钟源验证并只修复受影响区间。
但在 DN-008 的整手量化假设检验通过或完成独立逐笔/分钟复核前，100 只放量门禁保持关闭。
可复现证据见 `data/qc_report/reconciliation_investigation/`，完整调查记录见
`docs/RECONCILIATION_INVESTIGATION.md`。

## DN-008：整手量化假设、材料性与第二源抽查

若每根 5 分钟 volume 是精确股数四舍五入至 100 股，单根误差的绝对值不超过 50 股，
48 根的逐日硬上界为 2,400 股；独立均匀舍入的日误差标准差约为 200 股。实测结果否定
该模型对主要异常区间的解释：

- 5,203 个 volume 异常日的绝对差额中位数为 68,471 股，P95 为 1,536,960 股；只有
  1,761 日（33.85%）处于 2,400 股硬上界内。
- 2024 年的中位绝对差额为 215,292 股、P95 为 2,170,481 股，只有 79/2,177
  （3.63%）处于硬上界内。因此“逐 bar ±50 股舍入”不能解释 2024 年的主体异常。
- 档案在 2023-08-09 出现横截面同步切换：此前 8 只非科创板股票逐日对平，当日起
  每根 bar 同时变成 100 股整数倍并出现数万至数百万股差额。2025-03-20 后出现第二次
  切换：6 只创业板股票主要降到 ±50 股量级，但两只沪市主板仍有大额离群。
- 2026-06-17 全样本又同日短暂回到整手值；其中 9/10 只的差额处于 2,400 股内，
  `sh.688256` 仍少 143,239 股。故“整手精度限制”只解释部分 regime，不是统一模型。

材料性评估分开看待价格和成交量：

- 396 个末根 close 异常占 14,326 个覆盖股日的 2.76%。异常内的绝对差中位数为
  0.06 元、P95 为 0.7625 元、最大 7.92 元；相对误差中位数为 7.30 bps，150 日超过
  10 bps（占全部覆盖股日 1.05%）。因此不能将它们整体称为分币级舍入。
- 作为敏感性场景，将每日分钟量按 `daily_volume/minute_volume` 等比例校正，再重算
  “相对过去 20 日同一时段均量”。在 2023-08-09 至 2025-03-19，特征绝对变化 P95
  为 0.01034，截面 Rank 相关的 P5 为 0.9833；后续 regime 的 Rank 相关 P5 为 1.0。
  这支持相对量能/截面排序可能不敏感，但等比例校正不是逐 bar 真值，不能替代第二源验证。

东财 5 分钟端点通过 AkShare、限量直连和应用内浏览器均返回空响应，未能取得 2024 样本。
作为可获得的独立抽查，AkShare/Sina 最近 1,970 根 5 分钟线覆盖了 2026-06-17：

- `sh.601138`：Sina volume 与日线完全一致，Baostock 差 18 股；三方 close 一致。
- `sh.688256`：Sina volume/close 与日线完全一致，Baostock 少 143,239 股且 close 少
  0.04 元，确认该日 Baostock 分钟档案存在实质偏差。
- `sz.300308`：Baostock 只差 42 股，Sina 反而比日线少 18,700 股；三方 close 一致。

第二源本身也不是无条件真值。本轮当时结论是：取整假设只解释后期部分小差额，尚未通过
解锁门槛；全量换源不是唯一方案，需要独立样本或可验证的修复/屏蔽策略。后续 DN-009
已选择并验证后一路径，并取代本条目的临时门禁决定。可复现脚本为 `investigate_precision_materiality.py`，输出位于
`data/qc_report/precision_materiality/`。

## DN-009：乘法因子验证、研究用重标定与 100 只门禁

最后验证了“历史分钟 volume 被 CTO 复权因子错误缩放”的假设。测试区间为
2023-08-09 至 2025-03-19；`sh.688820` 尚未上市，实际覆盖 9 只：

- 每只股票的 `minute_volume/daily_volume` 与
  `minute_amount/daily_amount` 高度同步，相关系数为 0.9997–1.0000，确认差异具有
  日级乘法性，而非简单漏计一笔成交。
- 但 volume ratio 与 CTO 复权因子的正向或反向归一化变换均不匹配；9 只股票的已知
  因子解释数为 0。复权因子不变的平台内，volume ratio 几乎每天都不同，平台内 CV
  中位数为 0.33%–0.59%（个别平台更高），远超 0.01% 的精确逆变换门槛。
- `minute_close/daily_close` 与 volume ratio 共享同一比例的日占比仅为 0%–27.58%。
  因此 close 异常不是同一缩放因子的价格侧表现，不能随 volume 一并反变换。

结论：已知复权因子污染假设被否定；无法从该因子精确修复，但日线总量提供了可审计的
逐日缩放目标。研究用处理规则如下：

1. `data/raw_5min/` 永久保持原始数据不变；处理结果写入 `data/processed_5min/`。
2. 每个有交易日按原始 48 根 volume 权重分配日线 volume。为保持股数为整数，先向下
   取整，再按小数余数从大到小分配剩余股数；修复后 48 根之和严格等于日线 volume。
3. 处理文件保留 `volume_raw`，以修复后的 `volume` 作为研究字段，并增加
   `volume_scale_factor`、布尔列 `volume_rescaled` 和 `close_anomaly_flag`。
4. `close_anomaly_flag=True` 标记末根 close 与日线相差至少 0.01 元的整日全部 bar；
   下游价格类特征/标签应屏蔽这些股日，并报告包含与排除标记日的敏感性结果。
5. amount 暂不改写，继续保留严格异常和原始值。若下游使用成交额或 VWAP，需单独采用
   日线 amount 重标定并重新验证，不能默认沿用 volume 修复结论。

10 只试点共 687,648 根 bar、5,203 个 volume 异常股日完成处理；处理后日 volume
非零残差日为 0。396 个 close 异常股日全部写入屏蔽标记。量能特征材料性结果见 DN-008。
据此，100 只试点门禁调整为 **OPEN**，但正式研究入口必须使用处理目录而非原始目录。
可复现脚本为 `investigate_scaling_factor.py` 和 `repair_5min.py`，审计表为
`data/qc_report/volume_rescale_audit.csv` 与 `data/qc_report/scale_gate.csv`。

## DN-010：100 只扩池的停牌占位 bar 与日内缺根

100 只原始分钟数据共 6,937,529 根。OHLC 关系、重复、负值和非法时刻均为
0。完整性层发现两个 `tradestatus=1` 的真实日内缺根日：

- `sz.002384` 在 2023-12-21 只有 43 根，缺 09:35–09:55 的前 5 根。
- `sz.000021` 在 2024-06-06 只有 46 根，缺 09:35 和 09:40。

另有 64 个 `tradestatus=0` 的日期仍返回 48 根分钟 bar，但其逐日 volume 和
amount 之和均为 0；大部分 OHLC 也为 0，少数 bar 保留前收盘价。这是 Baostock
停牌占位 bar，不是实际交易或 `tradestatus` 冲突。质检将其分类为
`suspended_placeholder_bars`；与无 bar 的整日停牌一样，不进入研究特征和训练样本。

下游 `bar_index` 由 DN-001 的 48 个精确时刻映射，不由当日行号推断，因此缺失
早盘 bar 不会将当日余下时段编码整体左移。

## DN-011：100 只 regime 复核、标签覆盖与基线执行时点

100 只扩池后，DN-008 的横截面 regime 边界仍然稳定：2023-08-09 有 92 只具备
历史覆盖，其中 95.65% 同时进入大额 volume gap；2025-03-20 的大额 gap 占比降至
2.17%，较前一日下降 92.39 个百分点。92 只历史充分股票均判为 `STABLE`；另 8 只
为上市历史不足，不作为 regime 异常。证据表位于 `data/qc_report/regime_100/`。

研究特征共 6,934,357 根有效 bar。除允许保留的 `vol_rel_20d` 历史不足外，所有核心
特征 NaN 数为 0；`vol_rel_20d` NaN 率为 0.3447%。30/60 分钟标签 NaN 率分别为
12.5002% 和 25.0004%，与每日最后 6/12 根不跨日截断一致。14:35 是最后六根之一，
因此按既定定义不能生成 30 分钟标签；保留该采样点供序列和 60 分钟任务使用。

原始基线标签严格遵循 `log(close[t+6]/close[t])`。由于特征在 bar 收盘后才可计算，
该标签隐含按同一根 `close[t]` 成交，存在执行时点乐观性，但不是训练/测试穿越。
泄漏审计显示，高原始 RankIC 主要来自短周期反转：单信号 `-pos_in_bar` RankIC 为
0.0757、`-ret_5m` 为 0.0596，同刻随机置乱为 -0.0008。作为敏感性检验，以下一根
bar close 入场、仍在原 `t+6` 退出时，8 个窗口平均 RankIC 从 LightGBM 0.1016 / Ridge
0.0912 降至 0.0397 / 0.0297。正式策略解释应优先引用延迟入场结果；原规格结果保留
用于模型可预测性对照，并维持 `HIGH_REVIEW` 标记。

## DN-012：阶段三神经网络的时间门禁与主评估口径

MLP 与 LightGBM 使用完全相同的 25 个快照特征；GRU 使用 48×10 原始序列。每个滚动
窗口都只用训练段估计 NaN 填充值、均值和标准差，验证/测试数据不得参与。训练目标仍为
既定的截面 `label_rank`，但早停和主报告统一使用 DN-011 的下一根 bar 入场收益。

训练前的两个门禁均通过：5 次同采样时刻标签打乱后，LightGBM/Ridge 的延迟 RankIC
分别为 -0.00244±0.00449 和 -0.00083±0.00980；8 个窗口均满足前一段最大标签终点
早于后一段最早样本时刻，最小间隔 43.5 小时。序列可包含分割点之前的可观测历史上下文，
但该上下文不含前一段尚未实现的标签，故不视为穿越。

本地 CPU 的窗口一单 seed 结果仅用于流程验收：MLP 延迟 RankIC 0.02108，低于同窗口
LightGBM 的 0.02635；GRU 为 0.03436，暂时高出 0.00802。GRU 虽超过 0.005 增量线，
但在 Kaggle 完成 5 seeds 且种子标准差不超过 0.005 前，`sequence_value_add_pass` 保持
False，不作序列建模有效的最终结论。

## DN-013：研究产物持久化与“完成”定义

研究产物采用本地主副本、云端分发副本的双层持久化规则：

1. `data/archive/` 是研究结果的主副本目录。模型权重、预测、归因、成本层结果、报告、
   执行日志与清单必须以不可变版本归档到本地；云端 Kaggle Dataset 仅作为分发、计算恢复
   和异地冗余副本，不能替代本地主副本。
2. 每个本地归档必须保留云端 Dataset slug 与版本号、下载时间、压缩包字节数、SHA-256、
   压缩包成员数和解压后文件数。关键 CSV 必须通过实际解析校验，而非仅检查文件存在或
   ZIP CRC；至少记录行数、列名和读取状态。
3. 归档采用版本隔离目录，不允许用“latest”覆盖历史版本。Stage 4 的目录命名为
   `data/archive/stage4_dataset_v{version}/`，并保留原始整包 ZIP、解压文件和校验清单。
4. 任何“结果完成”“阶段完成”或 checkpoint 的定义都包含：主副本已经落地本地、文件数
   校验通过、关键 CSV 可读、清单已生成。只有云端运行完成、Dataset 上传成功或 Notebook
   显示成功，状态一律记为 `CLOUD_COMPLETE_LOCAL_PENDING`，不得对外表述为完成。
5. 本地归档校验失败时不得以云端可预览作为降级通过；应重新下载或恢复缺失成员，并在
   校验清单中保留失败记录和修复时间。

2026-08-09 已通过 Kaggle API 将 Dataset Version 16 与 17 的指定历史版本完整下载至
`data/archive/stage4_dataset_v16/` 和 `data/archive/stage4_dataset_v17/`。原始 ZIP 的成员数
分别为 178 和 163，与 API 文件清单及解压文件数完全一致；ZIP CRC 与 SHA-256 校验通过，
V16 的 41 个 CSV 和 V17 的 27 个 CSV 均已实际解析成功。V16 云端展开目录还原得到本地
`data/archive/stage4_cost_complete.zip`，包含 143 个文件并通过 CRC 校验。详细字节数、
SHA-256、关键 CSV 行列与列名见 `data/archive/STAGE4_ARCHIVE_MANIFEST.md`。Stage 4 持久化
状态现为 `COMPLETE_LOCAL_PRIMARY_VERIFIED`。
